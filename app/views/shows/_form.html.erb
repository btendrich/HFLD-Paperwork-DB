<%= form_with(model: show, local: true) do |form| %>
  <% if show.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(show.errors.count, "error") %> prohibited this show from being saved:
      </h2>

      <ul>
        <% show.errors.full_messages.each do |message| %>
          <li>
            <%= message %>
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, :class => "label" %>
    <%= form.text_field :name, :class =>"input" %>
  </div>

  <div class="field">
    <%= form.label :venue, :class => "label" %>
    <%= form.text_field :venue, :class =>"input" %>
  </div>

  <div class="field">
    <%= form.label :notes, :class => "label" %>
    <%= form.text_field :notes, :class =>"input" %>
  </div>

  <div class="actions">
    <%= form.submit :class => "button is-link" %>
  </div>
<% end %>